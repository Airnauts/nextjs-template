import { useContext } from 'react';
import { {{pascalCase name}}Actions, {{pascalCase name}}State } from './{{pascalCase name}}';

export function use{{pascalCase name}}State() {
  const context = useContext({{pascalCase name}}State);
  if (context === undefined) {
    throw new Error('use{{pascalCase name}} must be used within a {{pascalCase name}}Provider');
  }
  return context;
}

export function useArTextContextActions() {
  const context = useContext({{pascalCase name}}Actions);
  if (context === undefined) {
    throw new Error('use{{pascalCase name}} must be used within a {{pascalCase name}}Provider');
  }
  return context;
}
