export type {{pascalCase name}}ProviderProps = React.PropsWithChildren<{}>;

export type {{pascalCase name}}StateType = {
  count: number;
};

export enum {{pascalCase name}}ReducerAction {
  increment = 'increment',
  decrement = 'decrement',
}

export type {{pascalCase name}}ActionsType = {
  dispatch: React.Dispatch<{{pascalCase name}}ReducerAction>;
  doSomethingCool: () => void;
};
