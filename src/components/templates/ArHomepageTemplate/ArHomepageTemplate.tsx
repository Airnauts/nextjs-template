import React from 'react';
import { ArHomepageTemplateProps } from './ArHomepageTemplate.types';

export function ArHomepageTemplate({ children }: ArHomepageTemplateProps) {
  return <>{children}</>;
}
